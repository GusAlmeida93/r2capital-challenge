version: 2

sources:
  - name: landing
    database: "{{ env_var('TRINO_CATALOG', 'lakehouse') }}"
    schema: raw
    tables:
      - name: landing_file_manifest
      - name: landing_file_rejections
      - name: landing_stores_received
      - name: landing_sales_received
